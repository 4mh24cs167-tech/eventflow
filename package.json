{
  "name": "event-org",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build-all": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@supabase/supabase-js": "^2.103.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "playwright": "^1.59.1"
  }
}
